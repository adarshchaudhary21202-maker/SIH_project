import type {ApiError,AuditEvent,Case,Correction,CustodyEvent,Evidence,LoginRequest,TestSession,Tokens,User} from './models';
const base=(globalThis as any).__API_BASE__||localStorage.getItem('ncb.apiBase')||'http://127.0.0.1:8000';
const tokenStore={get value():Tokens|null{const x=sessionStorage.getItem('ncb.tokens');return x?JSON.parse(x):null},set value(x:Tokens|null){x?sessionStorage.setItem('ncb.tokens',JSON.stringify(x)):sessionStorage.removeItem('ncb.tokens')}};
export class ApiClient{private refreshing?:Promise<boolean>;async request<T>(path:string,init:RequestInit={},retry=true):Promise<T>{const access=tokenStore.value?.access_token;const r=await fetch(base+path,{...init,headers:{'Content-Type':'application/json',...(access?{Authorization:`Bearer ${access}`}:{ }),...(init.headers||{})}});if(r.status===401&&retry&&tokenStore.value&&await this.refresh())return this.request<T>(path,init,false);if(!r.ok){let b:any={};try{b=await r.json()}catch{};throw {status:r.status,message:b.detail||b.message||`Request failed (${r.status})`,fields:Array.isArray(b.detail)?Object.fromEntries(b.detail.map((x:any)=>[x.loc?.at(-1)||'form',x.msg])):undefined} as ApiError}if(r.status===204)return undefined as T;return r.json() as Promise<T>}private async refresh(){if(!this.refreshing)this.refreshing=(async()=>{try{const t=tokenStore.value;if(!t)return false;tokenStore.value=await this.request<Tokens>('/auth/refresh',{method:'POST',body:JSON.stringify({refresh_token:t.refresh_token})},false);return true}catch{tokenStore.value=null;return false}finally{this.refreshing=undefined}})();return this.refreshing}login(b:LoginRequest){return this.request<{message:string;development_otp?:string}>('/auth/login',{method:'POST',body:JSON.stringify(b)})}verifyOtp(badge_id:string,otp:string,device_id:string){return this.request<Tokens>('/auth/verify-otp',{method:'POST',body:JSON.stringify({badge_id,otp,device_id})})}resend(b:LoginRequest){return this.request<{message:string;development_otp?:string}>('/auth/resend-otp',{method:'POST',body:JSON.stringify(b)})}async establish(t:Tokens){tokenStore.value=t;return this.me()}me(){return this.request<User>('/auth/me')}async logout(){const t=tokenStore.value;try{if(t)await this.request('/auth/logout',{method:'POST',body:JSON.stringify({refresh_token:t.refresh_token})})}finally{tokenStore.value=null}}cases(){return this.request<Case[]>('/officer/cases')}createCase(b:Record<string,string>){return this.request<Case>('/officer/cases',{method:'POST',body:JSON.stringify(b)})}sessions(){return this.request<TestSession[]>('/officer/test-sessions')}createSession(b:Record<string,string>){return this.request<TestSession>('/officer/test-sessions',{method:'POST',body:JSON.stringify(b)})}endSession(id:string){return this.request(`/officer/test-sessions/${id}/end`,{method:'POST'})}createEvidence(b:Record<string,unknown>){return this.request<Evidence>('/officer/evidence',{method:'POST',body:JSON.stringify(b)})}qr(id:string){return `${base}/evidence/${id}/qr`}correction(id:string,b:Record<string,unknown>){return this.request(`/officer/evidence/${id}/corrections`,{method:'POST',body:JSON.stringify(b)})}supervisorEvidence(){return this.request<Evidence[]>('/supervisor/evidence')}supervisorCases(){return this.request<Case[]>('/supervisor/cases')}verifyEvidence(id:string){return this.request<{hash_valid:boolean;status:string}>(`/supervisor/evidence/${id}/verify`)}approveEvidence(id:string,reason:string){return this.request(`/supervisor/evidence/${id}/approve`,{method:'POST',body:JSON.stringify({reason})})}rejectEvidence(id:string,reason:string){return this.request(`/supervisor/evidence/${id}/reject`,{method:'POST',body:JSON.stringify({reason})})}corrections(){return this.request<Correction[]>('/supervisor/corrections')}reviewCorrection(id:string,ok:boolean,reason:string){return this.request(`/supervisor/corrections/${id}/${ok?'approve':'reject'}`,{method:'POST',body:JSON.stringify({reason})})}audit(){return this.request<AuditEvent[]>('/supervisor/audit')}custody(id:string){return this.request<CustodyEvent[]>(`/supervisor/evidence/${id}/custody`)}transfer(id:string,b:Record<string,string>){return this.request(`/officer/evidence/${id}/custody`,{method:'POST',body:JSON.stringify(b)})}}
export const api=new ApiClient();export const auth={get active(){return tokenStore.value!==null}};
/** Fetches the protected QR image with the bearer token; an <img src> cannot attach it. */
export async function qrSource(id:string):Promise<string>{const token=tokenStore.value?.access_token;if(!token)throw {status:401,message:'Session expired'} as ApiError;const r=await fetch(`${base}/evidence/${id}/qr`,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw {status:r.status,message:'Unable to retrieve evidence QR'} as ApiError;return URL.createObjectURL(await r.blob())}
