{
  "name": "ncb-evidence-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "test": "npm run build" },
  "devDependencies": { "typescript": "^5.7.3" }
}
